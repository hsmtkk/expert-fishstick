version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip install -r requirements.txt
run:
  command: flask --app main run --host 0.0.0.0 --port 8080
  env:
    - name: LAMBDA_URL
      value: https://www.example.com
  network:
    port: 8080
